dashboard.py uses streamlit to show a dashboard with the urls found indexed under leg.state.co.us.

Paste this into the terminal to run:

python -m streamlit run dashboard.py

scrape_domain.py puts urls into a spreadsheet.

url_index.py uses the webarchive API to create a txt file with all the indexed URLs listed.